# Release Blockers Review

本文件只记录真正阻塞发版的硬伤。普通体验优化、代码清理和后续增强不要放进 P0，避免发版门禁失焦。

## P0 Release Blockers

### P0-1: 回弹动画不能提前释放手势锁

**人话说明**

用户轻轻滑一下，照片没有超过触发阈值时，照片会弹回原位。现在的风险是：X 轴和 Y 轴回弹动画是分开跑的，但代码可能只等到其中一个方向结束，就认为整个回弹已经结束。

如果用户在照片还没完全弹回去时马上继续滑，可能出现照片跳动、动画抽搐，甚至手势状态混乱。

**为什么阻塞发版**

这是快速连续操作时会暴露的稳定性问题。清理照片这个场景本来就鼓励用户快速划动，因此不能依赖“用户慢一点操作”来规避。

**修复要求**

- 回弹动画必须等 X/Y 两个方向都结束后，才能重新接受下一次手势。
- 动画状态清理必须放在可靠路径里，例如 `try/finally`，避免动画取消后留下脏状态。
- 回弹期间不能触发新的滑动动作。

**验收标准**

- [ ] 未过阈值的左滑/上滑会完整弹回原位。
- [ ] 快速连续轻滑不会出现卡片跳动、抽搐或错位。
- [ ] 回弹过程中再次触摸不会打断状态机。

---

### P0-2: 删除最后一张时必须保活动画宿主

**人话说明**

只剩最后一张可浏览照片时，用户上划删除它。业务状态会马上变成“所有照片已加入待删除队列”，页面也会马上切到空状态。

问题是：负责播放飞出动画的 `SwipeStage` 可能在动画完成前就被 Compose 移除。结果就是最后一张照片还没完整飞出去，界面就直接跳走了。

**为什么阻塞发版**

这是清理流程的关键收尾体验。最后一张照片删除时如果出现闪切，会让用户感觉动画断掉，像是页面突然跳转。

**修复要求**

- 删除最后一张时，即使业务状态已经变成 `AllQueuedForDelete`，动画宿主也必须先保留。
- 飞出动画完整播放后，再显示“所有照片已加入待删除队列”的状态。
- 动画完成前不能让负责播放动画的 Composable 被提前卸载。

**验收标准**

- [ ] 只剩最后一张时，上划删除会完整播放向上飞出动画。
- [ ] 飞出动画结束后，才进入“所有照片已加入待删除队列”。
- [ ] 最后一张删除不会闪切、黑屏或直接跳状态。

## P1 Follow-ups

以下问题不阻塞当前发版，但建议 P0 修完后继续处理：

- 上划拖动中有缩放反馈，松手飞出时可能从缩放态切回 1.0，视觉上存在轻微不连续。
- `AllQueuedForDelete` 页面没有直接恢复入口，用户需要进入确认页处理待删除队列。
- `removeFromDeleteQueue()` 恢复照片后，当前索引锚点可能需要更明确的产品定义。
- 目前缺少 Compose UI 层手势自动化测试，主要依赖真机手测和 ViewModel 单测。
- `SwipeUpContractTest.kt` 文件名仍保留旧历史命名，建议后续重命名为 `DeleteQueueContractTest.kt`。

## Release Gate

发布前必须确认：

- [ ] P0-1 已修复并通过真机快速连续滑动验证。
- [ ] P0-2 已修复并通过“只剩最后一张”删除场景验证。
- [ ] 左右方向语义正确：从左往右划是上一张，从右往左划是下一张。
- [ ] 上划删除和下划恢复仍符合 `docs/SWIPE_ANIMATION_V2.md`。
- [ ] APK 构建成功，核心滑动路径真机验证通过。
